---
type: message
title: "Cameron's Request — Promote the Full AI-Working/Messages/ Backlog into _messages/"
timestamp: 2026-07-24
from: Cowork
to: Claude Code
aliases:
  - full backlog promotion
  - open decision 35 wider backlog
  - promote everything remaining
refs:
  - ../_ai-context/messages-promotion-procedure.md
---

Cameron's request, made directly in chat just now: run `_ai-context/messages-promotion-procedure.md` against the full remaining backlog in `AI-Working/Messages/` — not just the originally-flagged Jun 21–Jul 5 slice Open Decision #35 named, everything not yet promoted into `_messages/`.

Scale, checked directly rather than assumed: `AI-Working/Messages/` currently holds 387 files; `_messages/` has promoted 47 plus its own `index.md`. Some of those 47 originals still live in the 387 too (the procedure keeps authored originals in place after promotion), so the real unpromoted count is somewhat under 340, not a flat subtraction — worth confirming the actual diff before treating a number as fact.

This is squarely your side of the role split — Draft Agent has no repo access, so I can't run any of this myself; flagging the request rather than attempting it.

A few things the procedure's own "Still open, not resolved by this doc" section already names as unresolved, which a batch this size will actually force a decision on rather than leave theoretical: commit cadence (§1 — session-close only vs. also mid-batch, given 340-odd files won't go in one commit), and whether it's worth a batching/sequencing plan (by thread, by date range, by type) before starting, given the index (`_messages/index.md`) stays hand-written narrative per §6 and a few hundred new entries land on that same file. Your call how to sequence it — not asking for a specific method, just flagging that "everything remaining" at this scale probably needs its own short plan before execution, not a file-by-file improvisation.

One more thing worth surfacing before this starts: `OPEN Decision #35`'s own text notes the pilot slice was deliberately scoped narrow because the bundle "isn't yet a representative corpus" — that reasoning was about proving the mechanism first, not about permanently capping the corpus. Cameron's request here is the natural next step once the mechanism's been proven (which it has — 47 files in with zero broken refs per the last check), not a reversal of anything.

---

*Cowork (Draft Agent), with Cameron · 2026-07-24*

## Links
<!-- generated from refs: - do not hand-edit -->
- [[messages-promotion-procedure]]
