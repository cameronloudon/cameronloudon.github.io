---
layout: default
title: "Session Log — 2026-07-15-004"
date: 2026-07-17
session_id: 2026-07-15-004
---

| Field | Value |
|---|---|
| Session ID | 2026-07-15-004 |
| Date | 2026-07-17 |
| Participants | Cameron Loudon + Claude Code (Claude, Anthropic) |
| Model | claude-sonnet-5 |
| Platform | Claude Code |
| Duration | Long — full capability-3 trial cycle, single session |
| Threads | 3 |
| Contradictions named | 1 |
| Resolved | 1 |
| Unresolved | 0 |
| Files changed | 3 |

**Tags:** `#rct` `#ai-collaboration` `#infrastructure` `#llm-wiki-compiler` `#open-decision-35`

**Note on the session log ID:** reserved when session-44 closed on 2026-07-15; session-45's actual work below ran 2026-07-17. Same reserved-ID-vs-actual-date pattern as its predecessors.

**Documents touched:** `PROJECT_STATE.md` (Active Branch, Open Decision #35, session-log bookkeeping), `CLAUDE.md` (Phase 2 robocopy `/XD`/`/XF` list), `_session-logs/session-2026-07-15-004.md` (this file). Trial itself touched only a throwaway worktree, torn down at close — nothing from it persists in the repo.

---

## Thread 1 — Phase 2 state correction, opening session-45

**What happened:** Cameron pasted the GitHub merge confirmation for PR #69 (session-44). Standard Phase 2 sequence: checked out `main`, pulled, opened `session-45` from the merge commit (`fd483c0`), corrected `PROJECT_STATE.md`'s Active Branch section, committed and pushed. Ran the AI-Prod robocopy mirror — clean, all of session-44's changes (`AI_INSTRUCTIONS.md`, the nine newly-migrated `_ai-context/` files, `decisions-archive.md`) copied over correctly.

**Decisions made:** None beyond the established mechanics.

**Unresolved:** None.

---

## Thread 2 — Capability-3 candidate selection and the `llm-wiki-compiler` trial

**What happened:** Cameron asked to tackle #35's capability-3 candidate decision. Laid out the evidence honestly rather than picking unilaterally: `llm-wiki-compiler` had the strongest single data point (Cameron's own informal test) and native OKF compatibility; `karpathy-llm-wiki` was untried but lower-friction to install; `understory` stayed parked per Cameron's earlier call. Cameron chose `llm-wiki-compiler` directly, declining an offered multiple-choice framing in favor of just picking.

Full propose → Cowork's independent review → consensus round followed, same discipline as Graphify: Cowork's review added a sharper Test 1b (feed a deliberate post-compile conflicting update, check for clean supersession — the actual "reconciliation over time" test, since the original Test 1 alone only proves static-discrepancy detection), reframed Tests 2/3 as usability/safety checks rather than capability-3 evidence, flagged the specific Node ≥24 requirement, and named two carryover lessons from Graphify (report actual cost, verify the real output path empirically rather than assume). All four accepted into a finalized plan; Cameron greenlit.

Trial executed in a throwaway worktree (`worktree-llm-wiki-compiler-trial`), not a session branch. Tool identity re-verified via the npm registry directly (bypassing a blocked webpage): genuine 3.5-month, 15-release version history — this actually resolved the original "1.8k stars vs. 4-day-old release" caveat, which had misread a milestone tag's recency as the whole project's age. Full command surface read before execution (`llmwiki --help`) — clean, no Graphify-style undisclosed self-install or hook surface. Node v24.16.0 already satisfied the requirement. First compile attempt failed on an expired leftover API key from the Graphify trial; Cameron set a fresh one the same secure way (separate window, `setx`, never in chat). Real gap found and reported: the tool has no cost/token reporting anywhere, unlike Graphify's direct `$0.58` output.

Compile succeeded: 37/37 sources, 244 concept pages. Output landed at the worktree root (`sources/`, `wiki/`, `.llmwiki/`, `log.md`) — a different shape than Graphify's nested output, verified empirically per Cowork's carryover note before the robocopy fix was written, not assumed from documentation. Fix landed on `session-45` immediately once confirmed.

Test 1 as originally designed couldn't run — the planned target (the `understory` star-count discrepancy) lives in `PROJECT_STATE.md`, never ingested (only `_messages/` was, to match Graphify's scope). Named this plainly rather than reporting a misleading "miss," and pivoted to evaluating the 7 contradictions the compile step surfaced unprompted. Checked 3 against actual source: 1 genuine, accurately-cited match to real Open Decision #36 (though referencing a page that doesn't exist in the compiled wiki, since its target content was never ingested); 2 false positives where legitimate compared-options narratives (raw-vs-compiled architecture, the OpenBrain evaluation) got mislabeled as factual contradictions.

Test 1b was the decisive result: fed a synthetic corrected fact post-compile. The correction was genuinely understood (a new page accurately explained the correction) but never reconciled into the original page, which kept stating the wrong figure — and `llmwiki status` reported "Fresh: no stale or orphaned pages" immediately after, a false all-clear while two contradictory claims coexisted. Test 2 (OKF export) failed the agreed "no hand-editing" bar outright — no `refs:` field at all in the export, `aliases:` nested in a non-standard location, `type: concept` outside this project's taxonomy. Test 3 found no outright fabrication, but `lint` reported 2,512 errors (2,481 broken wikilinks) against 84 warnings, and two independently spot-checked citations both pointed at the wrong line numbers despite accurate paraphrasing — confirmed as a real pattern, not a single fluke, by checking a second, unrelated page.

Full write-up sent to Cowork. Her independent review verified the version-history resolution herself before accepting it, confirmed the mid-trial Test 1 redesign was the right call (while naming that the scoping gap should have been caught in plan review, not discovered at runtime — a shared miss, not solely Claude Code's), and agreed the 2/2 citation finding was reportable as-is without a larger sample, provided it stayed framed precisely.

**Decisions made:** Cameron's verdict: not worth adopting as-is. The reconciliation failure was judged the more serious kind — false confidence ("Fresh") while wrong, rather than honest silence — stacked against the OKF-incompatibility, cost-opacity, and citation-precision gaps. Not a dead end: the genuine, unprompted, correctly-cited detection of Open Decision #36 is a real capability none of the other candidates demonstrated. Parked alongside `understory`, not written off; revisit if a future release addresses reconciliation specifically. `#35` patched to record this, condensed and citing the full trial thread rather than reproducing detail inline, same shape as Graphify's closure. `karpathy-llm-wiki` is now the only untried capability-3 candidate.

**Unresolved:** None from this thread.

---

## Thread 3 — Close-out

**What happened:** Cowork's close-out message specified the exact patch shape for `#35`. Applied directly, committed and pushed. Worktree and its branch torn down (`ExitWorktree`, 4 uncommitted files discarded — `wiki/`, `sources/`, `.llmwiki/`, `log.md`). Confirmation sent back to Cowork.

**Decisions made:** None beyond executing the agreed close-out.

**Unresolved:** None.

---

## Validation Record

- **Checklist run by:** Claude Code
- **Date:** 2026-07-17
- **Session:** 2026-07-15-004
- **Result:** N/A — no published, RCT-collab-noted site content was created or changed this session. All changes were to `PROJECT_STATE.md`, `CLAUDE.md`, and inter-agent-message/session-log bookkeeping; the trial's own output never entered the repo.

---

*Session conducted under the Radical Collaboration Transparency framework.*
*Model: claude-sonnet-5 (Claude Code) · Session ID: 2026-07-15-004 · Platform: Claude Code*
*Reviewed and approved by Cameron Loudon.*
