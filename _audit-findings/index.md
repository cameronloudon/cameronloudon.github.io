---
type: index
title: "Audit Findings"
okf_version: "0.1"
---

Chain-of-custody record of Auditor findings, promoted from `AI-Working/Audit/` into git for diffable, tamper-evident history. Not rendered on the live site — excluded via `_config.yml`. Sibling to `_messages/`, not a subfolder of it — the Auditor's write boundary is a separate, path-scoped grant (`AI-Working/Audit/`), distinct from the Draft Agent's `Messages/` grant, and its findings get a separate durable home for the same reason.

Governing brief: `_ai-context/auditor-charter.md`. Findings use OKF `type: audit-finding` (charter §6) — the reserved fourth type, empty here until the Auditor's first real run.

## How findings get here

Cameron invokes an audit (fresh instance, the charter as its complete brief) → the Auditor writes its finding file to `AI-Working/Audit/` → the Publish Agent promotes it here, verbatim, on a session branch → Cameron reviews the diff and merges → the post-merge sync mirrors it to AI-Prod.

**Verbatim promotion, two mechanical checks, not one:** the promoting agent is one of the audited parties and never edits a finding's authored content — if a finding is wrong, the remedy is a response document in the normal flow, never a silent edit. Once the link convention (generated `[[wikilink]]` footers from `refs:`) is in force here too, "verbatim" means two separate diffs: (a) strip the marked generated footer — the remainder must be byte-identical to the staged original in `AI-Working/Audit/`; (b) regenerate the footer from the finding's own `refs:` — it must match what's present. Check (a) catches edits to the finding; check (b) catches footer tampering or generator faults.

## Findings

- [OpenWork Disqualified Under Point 1 — Cold-Walk Isolation Safeguard, Point-4 Empirical Check](./2026-07-18-openwork-point4-disqualification.md) — `blocks-independence`. First entry in this collection, and not from a running Auditor instance (none exists yet) — it's the safeguard's own point-4 verification step, run by Cameron directly per the independence rationale documented in its `provenance` field. OpenWork disqualified as a Function A candidate: confirmed autonomous cross-session context retrieval with no disable path found in the interface.
- [AnythingLLM Cleared With Conditions Under Point 1 — Cold-Walk Isolation Safeguard, Point-4 Empirical Check Complete](./2026-07-19-anythingllm-point4-cleared-with-conditions.md) — severity axis doesn't cleanly fit (see the finding's own Severity section); disposition is "cleared with conditions," not `blocks-independence` or `cosmetic`. Both items left open in the OpenWork-era entry (tool-disclosure check, fabrication stress-test) are now closed — one clean, one inconclusive-but-not-disqualifying. Clearance is conditional on the single-dedicated-workspace architecture being actually executed (deletion of prior test workspaces, confirmed permanent) before any real walk, plus standing pre-walk re-checks (Personalization off, no carried-forward chat history). Strongest results to date on either candidate: platform-enforced filesystem containment, and a citation-check probe where the model correctly separated known fact from flagged speculation.
- [Function A Cold Walk — Repeatability Confirmed, Four Findings on Git-State Access and Instruction-File Drift](./2026-07-26-function-a-cold-walk-repeatability.md) — mixed, 2 `blocks-independence` / 2 `cosmetic`. The first genuine cold-walk run since 2026-07-19, and the third of three attempts the same day after two rounds of kickoff-text hardening (Open Decision #47). Findings 1 and 2 are the substantive ones: the Auditor cannot verify `PROJECT_STATE.md`'s Active Branch claim or any other git-state fact (no `.git` in the AI-Prod mirror, real repo access denied), and `CLAUDE.md`'s File Structure section still names two files at root that moved to folder paths in sessions 19–20. **Editorial note on Finding 3:** it quotes `ONBOARDING.md` as containing "master-synthesis-prompt.html (repo root)" — checked directly against both the live repo and the AI-Prod mirror the Auditor actually read from, and that text does not exist in the file (item 3 in the real file is `PROJECT_STATE.md (repo root)`, which is accurate). The other half of Finding 3, the matching claim about `AI_INSTRUCTIONS.md` §2 step 6, checks out exactly as quoted. Per charter §6 the finding's own text is reproduced verbatim regardless — this note is the response-document remedy, not an edit to the finding — and the discrepancy has been raised with Cowork as a citation-accuracy gap distinct from the self-referential-claims check Round 2's hardening already covers.

The Auditor's first mission proper (the cold-walk pre-flight, charter §10) still hasn't run — all three entries above are safeguard verification and repeatability-confirmation work that had to happen first, not that mission itself.
